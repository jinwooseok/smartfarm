import API from "/templates/src/Utils/API.mjs";

// 맨 처음 파일 불러오는 함수
// 배열로 저장 ??
// 인자로 조건을 받아서 show에 넘기면 될듯?, default는 전체
const loadFileList = () => {
	// 파일 불러오는 부분

}

// 클릭한 조건 확인 및 html 수정 함수
const checkClickedCondition = () => {

}

// 파일 목록 보여주는 함수
const showFileList = () => {
		// 불러온 파일을 조건에 따라 보여주기
}

// 체크한 파일 확인 함수

// 파일 다운로드 함수

// 파일 삭제 함수

// 파일 검색 함수

// 페이지 이동 함수